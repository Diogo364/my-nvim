local ai_config = require("diconfig.plugins.mini-extensions.ai")
local align_config = require("diconfig.plugins.mini-extensions.align")

require("mini.ai").setup(ai_config)
require("mini.align").setup(align_config)
